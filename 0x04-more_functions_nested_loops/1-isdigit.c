#include "main.h"

**/
* _isdigit - check if numbers are 0 - 9
* @c: caracter to be cheked
*
* Return: '0' or '1'
*/
 
 int _isdigit(int c)
{
 		if (c >= '0' && <= '9')
			return (i);
		else
			return (0);
}

